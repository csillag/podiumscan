# Agent Rules

- Do not add Co-Authored-By lines to git commits.
- Always add rules to AGENTS.md, never create a CLAUDE.md.
- When using subagents, monitor for "Tool result missing due to internal error" or hangs. Check in every 60 seconds and clean up if necessary.
